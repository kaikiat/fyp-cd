apiVersion: v1
kind: PersistentVolume
metadata:
  name: fileserver-staging  # Change the name for different namespace
spec:
  capacity:
    storage: 1T
  accessModes:
  - ReadWriteMany
  nfs:
    path: /modelshare
    server: 10.140.72.210 # change to filestore ip
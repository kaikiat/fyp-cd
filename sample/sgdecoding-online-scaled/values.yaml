# Default values for sgdecoding-online-scaled.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ghcr.io/kaikiat/python-docker:latest
  pullPolicy: Always
